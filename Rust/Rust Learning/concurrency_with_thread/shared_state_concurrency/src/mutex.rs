//mutex (Mutual Exclusion):- It ensuring only one thread can access or modify the data at a time.

use std::sync::Mutex;

pub fn mutex_one_thread_can_access(){
    let n=Mutex::new("Jay");
    
    {

    }
}